---
- name: Install packages
  yum: name={{ item }} state=latest
  with_items:
    - emacs
    - screen
    - autoconf
    - automake
    - libtool
  when: ansible_distribution == "CentOS"

- name: Install packages
  pip:
    name: six
    state: latest
